# Eval v3 离线验收记录

## 2026 年 9 月 9 日提交前复验

源码基于 `6c34a81e750b418620c0bb5be148427fd1f68d80`。使用 Node 22.16.0 运行下列命令。

```bash
node --unhandled-rejections=strict --experimental-strip-types src/evals/v3/offline.ts
```

| 项目 | 结果 |
| --- | --- |
| 确定性契约测试 | 80 / 80 通过 |
| 合成场景 | 10 / 10 规则检查通过 |
| 场景语义质量 | 未评判，未启用真实裁判 |
| 实际模型请求 | 0 |
| 实际知乎业务请求 | 0 |
| 独立驱动退出码 | 0 |

合成场景中的语义成功数为 0，表示没有语义通过声明，不表示十条真实任务失败。契约测试中使用的模拟裁判仅验证评分代码。

测试覆盖预期与执行隔离、六类单步契约、完整流程、全部对话轮次、证据快照、来源错配、裁判缺失与无效输出、首次与重试分别记录、未知用量、取消、预算、自主策略协议、回归筛选及不可比报告拒绝。

## 集成验证边界

提交环境无法解析 github.com，未取得完整项目依赖。四项真实处理函数与 SQLite 集成测试已加入，提交前尚未在该环境执行。独立驱动结果不能代替完整仓库的 `vp check`、`vp test` 和 `vp build`。

完整仓库验收应查看 PR 的实际 CI 结果。真实模型和知乎业务评测由用户另外开启，不作为提交步骤自动运行。分支不自动合并。
